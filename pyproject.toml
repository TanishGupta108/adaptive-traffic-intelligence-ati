[project]
name = "adaptive-traffic-intelligence"
version = "0.1.0"
description = "Adaptive traffic intelligence environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv==0.2.0"
]

[project.scripts]
server = "server.app:main"